# Atuin shell history config
# Only non-default settings are listed here.
# Full reference: https://docs.atuin.sh/configuration/config/

# Execute command immediately on enter (press tab to edit instead)
enter_accept = true

[sync]
# Enable sync v2 for cross-device history sync
records = true
